# Dependencies for i3ctl
# argparse is included in the standard library since Python 2.7
i3ipc
# Additional dependencies
python-xlib
pydbus

# GUI dependencies (optional)
# Install with: pip install -r requirements.txt[gui]
PyQt6>=6.5.0